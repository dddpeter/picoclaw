# 2026-09-07

## MiMo-Code 借鉴分析（picoclaw）
- 调研了 /docs/myworks/MiMo-Code（小米 MiMoCode，opencode fork，TS/Bun），输出分析文档：`docs/design/mimo-code-borrowing-analysis.zh.md`（`make lint-docs` 通过）。
- 结论：值得吸收 4 项，按优先级：
  1. Try-Best 低收益循环检测（edit_repeat Jaccard / bash_retry / action_streak，滑动窗口，命中后止损+IM 通知）→ 建议落点 `pkg/agent/turn_health.go`。
  2. Bash 输出 Token Efficient 管线（progress/ansi/redact/longline + never-worse 守门 + 形状剪裁）→ 改造 `pkg/tools/shell.go` inline 路径，落盘保持原始输出。
  3. 动态指令下沉 message 层、条件精确触发 → 校准 `pkg/agent/agent_inject.go` 注入设计，保 prefix cache。
  4. Retry Coordinator 细节：tool-call 后 replaySafe 禁止重放 step；persistent network retry → 补强 `pkg/providers/error_classifier.go`/`cooldown.go`。
- 不借鉴：Orchestrator 子会话、QuickJS exec 微内核、跨 harness hand-off。
- 用户偏好：文档放 `docs/design/`，用 `.zh.md` 后缀（该目录允许无英文源文件）。
